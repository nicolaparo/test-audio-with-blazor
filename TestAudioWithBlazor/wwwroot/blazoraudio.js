var audio_play = (audio) => audio.play();
var audio_pause = (audio) => audio.pause();
var audio_setCurrentTime = (audio, timeInSeconds) => audio.currentTime = timeInSeconds;
var audio_getCurrentTime = (audio) => audio.currentTime;
var audio_getDuration = (audio) => audio.duration;